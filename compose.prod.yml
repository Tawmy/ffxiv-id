services:
  db:
    networks:
      - default

  iam:
    depends_on:
      db:
        condition: service_healthy
    environment:
      KC_HOSTNAME: new.ffxiv.id
    networks:
      - default
      - reverse-proxy
    ports:
      - "9010:9000"
    labels:
      caddy: new.ffxiv.id
      caddy.@restricted.1_not: remote_ip 138.199.150.245

volumes:
  db:
    name: ffxiv-id-db

networks:
  reverse-proxy:
    external: true